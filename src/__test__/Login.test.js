import {render, screen} from '@testing-library/react'
import {Login} from '../pages/Login'

test('conform renduring', ()=>{
    // render(<Login />);s
    // expect(screen.queryByText(/Login/)).toBeInTheDocument();
})   